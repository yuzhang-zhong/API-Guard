interface:
  display_name: "API Guardian"
  short_description: "Securely store and use API credentials"
  default_prompt: "Use $api-guardian to safely store an API key and run commands that need it."
